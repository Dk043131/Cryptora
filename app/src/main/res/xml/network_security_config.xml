<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Deny all cleartext (HTTP) traffic across the entire application -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production domain configuration with strict HTTPS -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">cryptora.app</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Debug overrides: permit localhost/10.0.2.2 development if needed, disabled in release -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
